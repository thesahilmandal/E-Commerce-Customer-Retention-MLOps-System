name: Continuous Deployment (CD) - Build & Push to ECR

# Only trigger this when code is merged/pushed to the main branch
on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  build-and-push:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Configure AWS Credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ vars.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, Tag, and Push Data Pipeline
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          REPO_NAME: ${{ vars.ECR_DATA_PIPELINE }}
        run: |
          docker build -t $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG -f pipelines/data_pipeline/Dockerfile .
          docker push $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG
          echo "Successfully pushed $REPO_NAME:$IMAGE_TAG"

      - name: Build, Tag, and Push Training Pipeline
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          REPO_NAME: ${{ vars.ECR_TRAINING_PIPELINE }}
        run: |
          docker build -t $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG -f pipelines/training_pipeline/Dockerfile .
          docker push $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG
          echo "Successfully pushed $REPO_NAME:$IMAGE_TAG"

      - name: Build, Tag, and Push Inference Pipeline
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          REPO_NAME: ${{ vars.ECR_INFERENCE_PIPELINE }}
        run: |
          docker build -t $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG -f pipelines/inference_pipeline/Dockerfile .
          docker push $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG
          echo "Successfully pushed $REPO_NAME:$IMAGE_TAG"

      - name: Build, Tag, and Push Monitoring Pipeline
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          REPO_NAME: ${{ vars.ECR_MONITORING_PIPELINE }}
        run: |
          docker build -t $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG -f pipelines/monitoring_pipeline/Dockerfile .
          docker push $ECR_REGISTRY/$REPO_NAME:$IMAGE_TAG
          echo "Successfully pushed $REPO_NAME:$IMAGE_TAG"